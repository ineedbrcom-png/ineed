PORT=3000
JWT_SECRET=super-secret-troque-isto
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Credenciais do Firebase Admin (use uma das opções)
# Opção A: Caminho para JSON da service account
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/ineed-firebase-admin.json

# Opção B: Inline (se preferir via Secret Manager/CI, não recomendo deixar local)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
